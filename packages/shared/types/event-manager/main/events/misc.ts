export interface MiscEvents {
  WINDOW_MINIMIZE
  WINDOW_CLOSE
}

export interface MiscEventsPayload {
  WINDOW_MINIMIZE
  WINDOW_CLOSE
}

export interface MiscEventsReturns {
  WINDOW_MINIMIZE
  WINDOW_CLOSE
}
